Phase 9 iter6 — PARTIAL

Focus test: ActiveActiveReplicationForHybridTest.testAAReplicationCanConsumeFromAllRegions[0]

Cumulative bugs fixed across all iterations: 3
  - Bug 1 (commit c5a859a2f, iter4): BoundedInMemoryPubSubTopic.consume() short-circuit
    on target<lowWaterMark blocked multi-consumer admin propagation. Fixed.
  - Bug 2 (commit ebcba6799, iter5): VeniceWriter constructed without
    PUB_SUB_PRODUCER_ADAPTER_FACTORY_CLASS in its Properties bag, so it fell through
    to ApacheKafkaProducerAdapterFactory and dialed in-memory broker URL with the Kafka
    wire protocol -> 30 s partitionsFor block. Fixed via PubSubClientsFactory.createFactory
    falling through System.getProperty before the hardcoded default + InMemoryPubSubBrokerFactory
    static initializer setting the four adapter-factory class system properties.
  - iter6 supplemental fix (commit 069e2e560): bounded queue capacity 10k -> 100k AND
    reportConsumerPosition tracking changed from MAX-of-positions to
    MIN-of-positions (with a per-thread WeakHashMap) so eviction can no longer drop
    messages a slow consumer hasn't read yet. This is a defensive correctness fix
    that does NOT close the remaining failure shapes in this iteration's runs but
    eliminates one whole class of races for future iterations.

Bugs remaining (1+ unresolved): admin-propagation flakiness AND/OR meta-store
data-not-visible flakiness in dc-0 under bounded broker.

Failure shapes still recurring (varies between runs of the same focus test):
  - Shape A (sometimes [0]): line 215 sendEmptyPushAndWait timeout in 150 s with
      `dc-0=NOT_CREATED Offline job hasn't been created yet` and
      `dc-1=ERROR too many ERROR replicas` OR `dc-1=3/3 partitions completed`.
      I.e., the version-creation admin message reaches dc-1 and gets fully
      processed (push completes in dc-1 servers) but dc-0 controller never
      creates the offline job for the user-store version. dc-0 admin consumer
      appears stuck OR the version-creation admin message never propagates to
      dc-0.
  - Shape B (sometimes [0], [2], [4], [6]): line 360 DVC `Store does not
      exist` with `MissingKeyInStoreMetadataException: Get null StoreMetaValue
      value for key {"keyStrings": [<storename>], "metadataType": 4}`. The
      thin client successfully routes through dc-0 router -> dc-0 server but
      the server returns null for STORE_CLUSTER_CONFIG. iter6 instrumentation
      confirmed each child controller writes ~7 PUT messages into its local
      meta-store-rt within 1 s of store creation, and the dc-0 server reaches
      pos=9 on the meta-store-rt within 9 s. The PUTs almost certainly include
      STORE_CLUSTER_CONFIG (3 messages of keyLen=53 + 3 of keyLen=37 +
      1 of keyLen=39 across positions 1-7, with position 0 being a SOS control
      message and position 8 an EOP control message). Yet the thin-client
      query 45 s later returns null. Either the server's RocksDB write isn't
      durable for that key by the time the query lands, OR the router routes
      to a server that doesn't have meta-store data, OR the meta-store
      version isn't yet ONLINE in Helix from dc-0 router's perspective.

Diagnostic instrumentation added in iter6 (left in place for iter7):
  - BoundedMockInMemoryProducerAdapter.sendMessage now writes a
    `produce meta-store-rt` line to /tmp/aa-phase9-iter5-debug.log for every
    produce into a `venice_system_store_meta_store_*_rt` topic, with msgType,
    isControlMessage, keyLen, partition, offset. Use this to verify the
    controller actually wrote STORE_CLUSTER_CONFIG and at what offset.
  - All instrumentation from iter5 (createTopic, getPartitionCount MISS,
    getNumberOfPartitions, RT-only subscribe, FIRST-READ) still in place.

Why this is PARTIAL not FAILED:
  Real progress was made: Bug 2 fix (iter5) and Bug 1 fix (iter4) both proven
  by the failure timing changing dramatically (iter4: failed at line 246 in
  70 s; iter5/6: reaches lines 215/268/360 in 40-172 s). Adding the iter6
  capacity bump and MIN-watermark tracking removes one possible race even
  though it didn't close the remaining shapes.

  The 5-same-shape stop condition does NOT yet apply: shapes A and B alternate
  across runs.

Stage A test counts after iter6 (focus test only, 4 variants):
  - testAAReplicationCanConsumeFromAllRegions[0]: alternating between Shape A
    and Shape B failures (45-172 s).
  - testAAReplicationCanConsumeFromAllRegions[2,4,6]: consistently Shape B
    failure (~40 s each).
  - Other AA tests in the suite: not re-run.

Stage B (benchmark): not attempted.

Files unchanged (verified via git diff --stat):
  - InMemoryPubSubBroker.java, InMemoryPubSubTopic.java (criterion 2 invariant)
  - All MockInMemory* classes (criterion 2 invariant)
  - All AbstractPollStrategy/RandomPollStrategy and Phase 0-7 instrumentation

Files changed in iter6 (commit 069e2e560):
  - internal/venice-test-common/src/main/java/com/linkedin/venice/pubsub/mock/BoundedInMemoryPubSubTopic.java
  - internal/venice-test-common/src/main/java/com/linkedin/venice/pubsub/mock/adapter/producer/BoundedMockInMemoryProducerAdapter.java

Diagnostic file (overwritten on each run):
  - /tmp/aa-phase9-iter5-debug.log

iter7 next-step recommendation:

  1. Investigate Shape B (line 360 DVC) more deeply by ALSO instrumenting:
     (a) the dc-0 server's meta-store ingestion task: log every key processed
         from meta-store-RT, especially the keyStrings+metadataType, and
         the corresponding RocksDB put. Add the log inside
         StoreIngestionTask or VeniceMessageProcessor at the point right
         BEFORE the storage-engine put. Verify STORE_CLUSTER_CONFIG with
         the right storeName actually reaches RocksDB.
     (b) the dc-0 router's HelixReadOnlyStoreRepository: log when the meta
         store first appears in the local cache (this proves the router knows
         the store exists). If the router doesn't have the meta store cached
         at the time of the thin-client query, that's the bug.
     (c) the thin-client side: in
         `ThinClientMetaStoreBasedRepository.getStoreMetaValue` log the
         returned future-result before declaring null, so we can distinguish
         "server returned null" from "future timed out and hit the catch".

  2. Investigate Shape A (line 215 admin propagation) by adding a "produce
     to admin topic" instrumentation in BoundedMockInMemoryProducerAdapter
     similar to the meta-store-rt one, and a "consumed from admin topic"
     log in BoundedMockInMemoryConsumerAdapter that fires on every poll
     (not just FIRST-READ). Verify whether admin messages are produced AT
     ALL by the parent for the missing version, and whether dc-0's admin
     consumer is polling and seeing them.

  3. If Shape A has admin messages flowing but dc-0's controller doesn't act
     on them, the issue is upstream of the broker (in
     AdminConsumerService/ParticipantStateTransitionAvroSerializer or
     somewhere similar). Probably NOT a bounded-broker bug. Consider
     spotting any places where the controller assumes a specific Kafka
     broker behaviour that the in-memory mock does not provide (e.g. exact
     message ordering across partitions, pause-on-rebalance semantics,
     consumer-group coordination).

  4. Consider whether the focus test class's @BeforeClass setUp leaves
     some state from one method to the next that is not properly reset for
     in-memory broker (e.g. Helix cluster state from prior test). If so,
     try @AfterMethod resetting hooks. (But test code is in the
     no-modify list -- this would have to be a controller / cluster-wrapper
     fix.)

Artifacts:
  - aa-phase9-progress.md (chronological)
  - aa-phase9-iter6-focus.log (this iteration's last focus-test gradle log)
  - aa-phase9-iter5-focus.log (still in working dir for reference)
  - /tmp/aa-phase9-iter5-debug.log (file-based diagnostic events from last
    run; will get overwritten on each run)
  - HEAD = 069e2e560 on branch haoxu07/aa-ingestion-benchmark
