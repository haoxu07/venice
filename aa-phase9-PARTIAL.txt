Phase 9 iter5 — PARTIAL

Focus test: ActiveActiveReplicationForHybridTest.testAAReplicationCanConsumeFromAllRegions[0]

Bugs fixed this iteration: 1
  - Bug 2: VeniceSystemProducer's writer factory was constructed without
    PUB_SUB_PRODUCER_ADAPTER_FACTORY_CLASS in its Properties bag, so it
    fell through to the hardcoded ApacheKafkaProducerAdapterFactory
    default, which dialed the in-memory broker URL with the Kafka wire
    protocol and blocked for 30 s.
    Fix (commit ebcba6799):
      - PubSubClientsFactory.createFactory now falls through to
        System.getProperty before the hardcoded default.
      - InMemoryPubSubBrokerFactory static initializer sets the four
        adapter factory class system properties to the in-memory
        implementations (only when not already set; preserves Apache
        Kafka behaviour for explicit kafka tests).

Bugs remaining (1+ unresolved): admin-propagation flakiness and/or
data-not-consumed flakiness in the bounded broker.

Failure shape that's still recurring (varies between runs):
  - Sometimes line 215 `sendEmptyPushAndWait did not succeed in 150 s`
    with `dc-0=NOT_CREATED Offline job hasn't been created yet`
    (admin-propagation-to-dc-0 timing).
  - Sometimes line 268 `Servers in dc-0 haven't consumed real-time data
    from region dc-0 for key dc-0_key_4` (consumer-side timing).
  - Sometimes line 360 `Da Vinci client failed to start, Store does not
    exist` (meta-store thin-client lookup; this was the *furthest*
    progress observed).

Why this is PARTIAL not FAILED:

  Bug 2's fix is solid and confirmed by file-based diagnostic logging.
  The test now reaches phases of the workload it never reached before
  (it used to fail at line 246 in 70 s; it now reaches lines 268, 360
  in 70-172 s). That's substantive progress.

  The remaining flakiness has variable shape between runs, so the
  iteration loop's "5 same-shape" stop condition does not apply yet.
  But I'm out of budget to keep chasing it.

Stage A pass count after iter5:
  - testActiveActiveStoreRestart (single test) PASSED in iter3 (30 s).
  - testAAReplicationCanConsumeFromAllRegions: still failing with
    variable shape, but progresses much further than iter4.
  - Other AA tests in the suite: not re-run this iteration.

Stage B (benchmark): not attempted.

Diagnostic logging deliberately left in the code so a follow-up
subagent doesn't have to re-derive. Writes to
/tmp/aa-phase9-iter5-debug.log. To remove for prod, search for
`debugLog(` and `[bench-iter5]` in:
  - BoundedMockInMemoryProducerAdapter
  - BoundedInMemoryPubSubBroker
  - BoundedMockInMemoryConsumerAdapter

Next agent recommendation: Take the line 360 (DVC `Store does not exist`)
failure as the focus, since it's the furthest progress and the cleanest
shape (NativeMetadataRepository.fetchStoreFromRemote can't find the
store in the meta store via the thin client). Investigate why the
thin-client meta-store path doesn't see the store. Likely a configuration
gap on the DVC side — the test instantiates the DVC factory inline and
its pubsub/router config may not pick up the adapter factory system
properties correctly.

Artifacts:
  - aa-phase9-progress.md (chronological, includes iter5 analysis)
  - aa-phase9-iter5-focus.log (last focus-test gradle log)
  - /tmp/aa-phase9-iter5-debug.log (file-based diagnostic events from
    last run; will get overwritten on each run)
  - Commit ebcba6799 on branch haoxu07/aa-ingestion-benchmark.
